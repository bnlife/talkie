# 进度

## 总计划

- [x] 1. 骨架：项目搭好 Rust 能编译，前端能渲染
- [x] 2. 设置页面：配置读写 + 测试连接
- [x] 3. 对话核心：发送消息 + 流式接收 + 纯文本显示
- [x] 4. 对话管理：侧栏 + Tab 切换 + CRUD + 置顶 + 重命名
- [x] 5. 多 Provider 支持 + 提示词模板
- [x] 6. UI 打磨：三页面侧栏统一、自动保存、参数控制
- [x] 7. Markdown 渲染（marked + DOMPurify + CSS 变量预留）
- [x] 8. Token 消耗显示（API 返回 total_tokens）
- [x] 9. 对话自动标题（第一条助手回复提取）
- [x] 10. 消息自动滚动（原生 overflow-y-auto）
- [x] 11. 链接系统浏览器打开（Tauri open_url command）
- [x] 12. MCP 框架 + 博查搜索集成

---

## 当前状态

**测试：** 前端 175 + Rust 14 (store) + 20 (bridge) = 209 个测试全部通过

**最新提交：** `a0b6af3 refactor: 拆分长文件 McpView.vue 和 chat.rs`

---

## 已解决的问题

### 基础设施
- 骨架：Rust + Vue + shadcn-vue + Tauri v2
- 设置页面（多 Provider：CRUD / 启用禁用 / 模型管理 / 测试连接）
- 流式输出（llm.rs + SSE + CancellationToken 中断）
- 全栈日志（Rust + 前端，前端统一走 bridge 转发）
- UI 统一（toast 提示 / 错误弹窗自动关闭）
- 项目重命名 chatbot → talkie
- 侧栏打磨（搜索 / 右键菜单 / 置顶 / 选中态 / 内联重命名）
- 提示词模板管理（CRUD / 设为默认 / @blur 自动保存）
- 多 Provider（per-conversation 绑定 / 模型切换器 / 旧配置迁移）
- Temperature + Top-P 参数控制
- llm.rs 测试（mockito 验证请求体含 system prompt / temperature / top_p / headers）
- chat.rs 重构（提取公共逻辑，消除 send_message/regenerate_message 重复）

### Markdown
- marked + DOMPurify 渲染（流式全量重解析）
- CSS 变量预留样式调整接口（--md-* 变量）
- 链接 target="_blank" + 系统浏览器打开

### 对话体验
- 自动标题（第一条助手回复提取，去 markdown 标记，截断 30 字）
- 自动滚动（原生 div + 用户上翻暂停）
- Token 消耗显示（助手消息底部 xxx tokens）
- 搜索按钮（ChatInput 左下角，对话级开关）

### MCP 框架
- JSON-RPC 2.0 协议（序列化/反序列化/10 个测试）
- MCP 进程管理（spawn/stdin-out/initialize/shutdown/stderr 捕获）
- 进程池（start/stop/is_running/list_tools/call_tool）
- MCP 管理页面（市场分类/卡片列表/安装弹窗/启动中状态）
- 异步 toggle（后台线程 spawn + panic 捕获 + mcp:started/mcp:error 事件）
- 内置博查搜索 MCP server（mcp-servers/bocha-search/index.js）
- 搜索集成（perform_search → MCP call_tool → system role 注入）
- 集成测试（5 个，mock MCP server 端到端）

### MCP 市场数据
- 内置 16 个常用 server（文件/数据库/搜索/开发/云/通讯/生产力）
- 博查搜索（自建 local:bocha-search，无需 npm 包）

### 代码重构 (2026-07-02)
- stores.spec.ts 拆分（888行 → 4个独立测试文件 + helpers.ts）
- McpView.vue 拆分（516行 → 6个组件：Sidebar/MarketGrid/InstanceDetail/InstallDialog/CustomDialog）
- chat.rs 拆分（587行 → chat/engine.rs + chat/search.rs + commands/chat.rs）
- store.rs 拆分（1209行 → store/mod.rs + migrations.rs + chat.rs + prompt.rs + mcp.rs）

---

## 待开发

- [ ] MCP 样式调整页面（Phase 2：可视化参数 + 实时预览）
- [ ] 知识库功能（RAG：embedding + 向量存储 + 相似度搜索）
- [ ] 系统托盘
- [ ] 快捷键
