# 开发进度

## 总计划（5 步）

- [x] 1. 骨架：搭项目，Rust 能编译，前端能渲染
- [x] 2. 设置页面：配置读写 + 测试连接
- [x] 3. 单对话聊天：发送消息 + 流式接收 + 纯文本显示
- [x] 4. 多对话管理：侧栏 + Tab 切换 + CRUD + 置顶 + 搜索
- [ ] 5. 打磨 + 多 Provider 设置

---

## 当前进度

**当前步骤：** Step 4 完成，下一步打磨 + 多 Provider 设置

**最后操作：** 侧栏打磨完成（搜索/右键菜单/置顶/重命名写入）

---

## 已交付功能

- ? 骨架（Rust + Vue + Naive UI + Tauri v2）
- ? 设置页面（配置读写 + test_connection 真实 HTTP）
- ? 流式聊天（llm.rs + SSE + CancellationToken 中断）
- ? 全模块日志（Rust + 前端，前后端统一输出）
- ? UI 反馈（toast 提示 + 设置弹窗自动关闭）
- ? 项目重命名 chatbot → talkie
- ? 图标替换（对话气泡 SVG，Tauri 官方工具生成）
- ? 侧栏打磨（搜索框 / 右键菜单 / 置顶 / 选中态 / 重命名写入）
- ? 数据库迁移（pinned 列兼容旧库）
- ? 79 条测试全部通过

---

## 已确认的关键设计

- ? 模型管理：内置 DeepSeek + 自定义
- ? IPC 方案：Tauri Event 广播，切 Tab 不中断
- ? SQLite：两个表，删对话级联删消息
- ? AppState：SQLite + 配置 + 中断开关，Mutex 锁
- ? 错误码格式：ERR_全小写
- ? 对话标题：LLM 异步生成
- ? 日志规范：通用 skill，前后端分节
- ? 前端架构：bridge / stores / components 三层，API 化

