# 聊天机器人项目方案

## 一句话

一个桌面聊天机器人，支持多模型 API、多对话、流式输出、SQLite 存储。不写 markdown 渲染，不做 RAG。

---

## 技术栈

| 层 | 技术 |
|----|------|
| 前端框架 | Vue 3 + TypeScript |
| UI 组件库 | shadcn-vue（Reka UI + Tailwind CSS v4） |
| 状态管理 | Pinia |
| 图标库 | lucide-vue-next |
| 桌面壳 | Tauri v2 |
| 后端 | Rust |
| 数据库 | SQLite（rusqlite） |
| 通信 | Tauri IPC（invoke + event） |
| 测试 | vitest + @vue/test-utils |

---

## 架构

### 前端三层

```
bridge/    ← 调 Tauri invoke，纯函数，不持状态
stores/    ← 持有响应式数据（Pinia），调 bridge
pages/     ← 容器组件，串 bridge + store + components
components/← 展示组件，只认 props + events，不知道后端存在
```

### 数据流向

```
组件事件 → dispatch → store → 调 bridge → Tauri invoke → Rust
                                                          → Rust 推 event
                                                          → bridge listener
                                                          → store 更新
                                                          → 组件刷新
```

---

## 目录结构

```
chatbot/
├── package.json
├── vite.config.ts
├── tsconfig.json
├── components.json           ← shadcn-vue 组件配置
│
├── src/
│   ├── main.ts               ← 入口：注册 Pinia，挂载 App
│   ├── App.vue               ← 根布局：Toolstrip + 页面切换
│   │
│   ├── types/
│   │   └── index.ts          ← Message、Conversation、Settings 类型
│   │
│   ├── bridge/               ← 通信层（唯一调 invoke 的地方）
│   │   ├── chat.ts           ← sendMessage / stopStream / getMessages
│   │   ├── conversation.ts   ← list / create / delete / rename / pin / unpin
│   │   ├── settings.ts       ← getSettings / updateSettings / testConnection
│   │   └── log.ts            ← 日志桥接
│   │
│   ├── stores/               ← 状态层
│   │   ├── chatStore.ts      ← conversations、messages、streamingId、activeConversation
│   │   └── settingsStore.ts  ← baseUrl、apiKey、model、temperature、darkMode
│   │
│   ├── pages/                ← 页面组件（知道 store 和 bridge）
│   │   ├── chat/
│   │   │   ├── ChatView.vue      ← 聊天页面主容器
│   │   │   ├── Sidebar.vue       ← 对话侧栏（搜索/排序/右键菜单/重命名）
│   │   │   ├── MessageList.vue   ← 消息列表（ScrollArea + 自动滚底）
│   │   │   ├── MessageItem.vue   ← 单条消息气泡
│   │   │   └── ChatInput.vue     ← 输入区域（Textarea + 发送/停止按钮）
│   │   ├── knowledge/
│   │   │   └── KnowledgeView.vue ← 知识库页面（占位）
│   │   └── settings/
│   │       ├── SettingsView.vue  ← 设置页面容器
│   │       └── SettingsPanel.vue ← 设置表单
│   │
│   ├── components/           ← 展示组件（不知道 store 和 bridge）
│   │   ├── app/
│   │   │   └── Toolstrip.vue    ← 左侧工具条（导航 + 夜间模式切换）
│   │   └── ui/                  ← shadcn-vue 组件（通过 CLI 添加）
│   │       ├── avatar/
│   │       ├── badge/
│   │       ├── button/
│   │       ├── card/
│   │       ├── dialog/
│   │       ├── dropdown-menu/
│   │       ├── input/
│   │       ├── label/
│   │       ├── scroll-area/
│   │       ├── separator/
│   │       ├── slider/
│   │       └── textarea/
│   │
│   ├── lib/                  ← 工具函数
│   │   └── utils.ts          ← cn() 合并 class
│   │
│   ├── styles/
│   │   └── global.css        ← CSS 变量定义（light/dark 主题）
│   │
│   └── __tests__/            ← 前端测试
│       ├── setup.ts
│       ├── bridge.spec.ts
│       ├── stores.spec.ts
│       └── components/
│           ├── ChatInput.spec.ts
│           ├── MessageList.spec.ts
│           ├── SettingsPanel.spec.ts
│           └── Sidebar.spec.ts
│
└── src-tauri/
    ├── Cargo.toml
    ├── tauri.conf.json
    ├── capabilities/
    │   └── default.json      ← Tauri 权限配置
    └── src/
        ├── main.rs
        ├── lib.rs             ← Tauri Builder，注册所有 command
        ├── models.rs          ← Message、Conversation、Settings struct
        ├── error.rs           ← AppError 统一错误类型
        ├── commands/
        │   ├── chat.rs        ← send_message / stop_stream / get_messages
        │   ├── conversation.rs← list / create / delete / rename / pin / unpin
        │   └── settings.rs    ← get / update / test_connection
        ├── store.rs           ← SQLite CRUD
        ├── llm.rs             ← HTTP 客户端 + SSE 解析 + 中断
        └── config.rs          ← JSON 配置读写
```

---

## 页面布局

### 整体结构

```
┌─────────────────────────────────────────────────────────┐
│ Toolstrip │            Header (bg-muted)                │
│ (bg-muted)│  [标题]              [🌙][—][□][×]          │
│           ├─────────────────────────────────────────────┤
│           │ ┌─────────────────────────────────────────┐ │
│           │ │         Content Area (rounded border)    │ │
│           │ │  ┌─────────┬───────────────────────────┐│ │
│           │ │  │ Sidebar │      Main Content         ││ │
│           │ │  │(bg-back │      (bg-background)      ││ │
│           │ │  │ ground) │                           ││ │
│           │ │  └─────────┴───────────────────────────┘│ │
│           │ └─────────────────────────────────────────┘ │
└─────────────────────────────────────────────────────────┘
```

### 设计规范

- **外围区域**（Toolstrip + Header）：`bg-muted`
- **内容区域**：`bg-background` + `rounded-lg border` + `p-1` 间距
- **分割线**：不使用布局分割线，仅靠圆角边框分隔
- **hover 效果**：`hover:bg-background`（在 muted 上浮起）或 `hover:bg-foreground/5`（在背景上轻微变化）
- **选中态**：`bg-background text-foreground shadow-sm`

### 字号规范

- `text-sm`（14px）：主要内容、标题、输入框、侧边栏
- `text-xs`（12px）：暂不使用

### 主题色（CSS 变量）

```css
:root {
  --background: hsl(0 0% 100%);      /* 主背景 */
  --foreground: hsl(0 0% 9%);        /* 主文字 */
  --muted: hsl(0 0% 92%);            /* 外围背景 */
  --accent: hsl(0 0% 96%);           /* hover 背景 */
  --border: hsl(0 0% 90%);           /* 边框 */
}
```

---

## 功能清单

### 已完成

- [x] 设置页面：配置读写、测试连接
- [x] 单对话聊天：发送消息、流式接收、显示纯文本
- [x] 多对话管理：侧栏 + 切换、对话 CRUD、置顶/取消置顶
- [x] 对话标题自动生成
- [x] 夜间模式切换
- [x] 窗口控制（最小化/最大化/关闭）
- [x] 统一页面布局（Toolstrip + Header + 圆角内容区）

### 待开发

- [ ] 知识库功能
- [ ] Markdown 渲染
- [ ] 系统托盘
- [ ] 快捷键
- [ ] RAG

---

## 流式输出

### 发送流

```
前端 invoke("send_message", { conversation_id, content })
  → Rust:
      1. 存 user message 到 SQLite
      2. 构造 LLM HTTP 请求（含历史消息）
      3. 通过 reqwest 发 SSE 请求
      4. 逐行读取响应，每行是一个 chunk
      5. 通过 app.emit("chat:stream-chunk", payload) 推前端
      6. 流结束时 emit("chat:stream-done")
  → 前端监听事件：
      chat:stream-chunk → store 追加 streamingContent
      chat:stream-done  → 归档完整消息
```

### 中断流

```
前端 invoke("stop_stream")
  → Rust 设置 CancellationToken
  → LLM 请求的异步任务检测到取消 → 关闭连接
  → emit stream-done（标记被中断）
```

---

## SQLite 表结构

### conversations 表

```sql
id            TEXT PRIMARY KEY
title         TEXT DEFAULT '新对话'
model         TEXT DEFAULT ''
system_prompt TEXT DEFAULT ''
created_at    INTEGER NOT NULL
updated_at    INTEGER NOT NULL
pinned        INTEGER DEFAULT 0
```

### messages 表

```sql
id              TEXT PRIMARY KEY
conversation_id TEXT NOT NULL REFERENCES conversations(id) ON DELETE CASCADE
role            TEXT NOT NULL CHECK(role IN ('user', 'assistant', 'system'))
content         TEXT NOT NULL
created_at      INTEGER NOT NULL
token_count     INTEGER
```

---

## 测试

### 前端测试

```
src/__tests__/
├── setup.ts                  ← 测试环境配置
├── stores.spec.ts            ← chatStore + settingsStore 测试
├── bridge.spec.ts            ← bridge 层测试（mock invoke）
└── components/
    ├── ChatInput.spec.ts     ← 发送事件 / 禁用态 / Enter 发送
    ├── MessageList.spec.ts   ← 空状态 / 消息数量 / 流式态
    ├── SettingsPanel.spec.ts ← 更新事件 / 校验
    └── Sidebar.spec.ts       ← 搜索 / 选择 / 创建 / 重命名
```

运行命令：
```bash
npm run test          # 单次运行
npm run test:watch    # 监听模式
npm run typecheck     # 类型检查
```

---

## 开发命令

```bash
npm run dev           # 启动前端开发服务器
npm run build         # 构建前端
npm run tauri dev     # 启动 Tauri 开发模式
npm run tauri build   # 构建桌面应用
```
